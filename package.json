{
  "name": "storage-service",
  "version": "1.0.0",
  "description": "A web service that stores data from POST requests and returns it on GET requests",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
